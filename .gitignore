# Build/extract artefacts
*.zip
.extract/

# Enumeration output (oscp-enum.sh writes per-target logs)
loot/
scans/
*.log
nmap-*.txt

# Editor / OS cruft
.DS_Store
*.swp
*~

# enum.sh per-target output
enum_*/

# test harness (jsdom is installed on demand, never committed)
node_modules/
package-lock.json

# Claude Code session state and worktrees
.claude/
